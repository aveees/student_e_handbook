<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BulSUStudentEHandbook"
        tools:targetApi="31">

        <!-- Splash screen as launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other screens -->
        <activity android:name=".GetStartedActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".HistoryActivity"/>
        <activity android:name=".GeneralProvisionsActivity"/>
        <activity android:name=".InstitutionalPhilosophyActivity" />
        <activity android:name=".PolicyStatementsActivity" />
        <activity android:name=".ClassificationOfStudentsActivity" />
        <activity android:name=".StudentRightsObligationsActivity" />
        <activity android:name=".ObedienceToLawsActivity" />
        <activity android:name=".StudentsOrientationActivity" />


        <activity android:name=".HymnActivity"/>
        <activity android:name=".AcademicRegulationsActivity"/>

    </application>

</manifest>
